import Styled from 'styled-components';

const Page = Styled.main`
    width: 80%;
    margin: auto;
    position: absolute;
    top: 10%;
    left: 50%;
    transform: translateX(-50%);
`;

export default Page;